# 🔄 Data Flow

## 1. Gladiator Registration Flow (Apply Form)

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Browser   │───▶│  ApplyForm  │───▶│ /api/register│───▶│    Neon    │
│   (User)    │    │    .jsx     │    │   route.js   │    │  Database  │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
                                              │
                                              ▼
                   ┌─────────────────────────────────────────┐
                   │           PARALLEL OPERATIONS           │
                   │  ┌─────────────┐  ┌─────────────────┐  │
                   │  │ Create      │  │ Generate Avatar │  │
                   │  │ Registration│  │ Token           │  │
                   │  └─────────────┘  └─────────────────┘  │
                   └─────────────────────────────────────────┘
                                              │
                                              ▼
                   ┌─────────────────────────────────────────┐
                   │         IMMEDIATE EMAIL SEND            │
                   │  ┌─────────────┐  ┌─────────────────┐  │
                   │  │ Get Drop    │  │ Generate Email  │  │
                   │  │ Info        │  │ (Groq AI)       │  │
                   │  └─────────────┘  └─────────────────┘  │
                   │              ▼                         │
                   │  ┌─────────────────────────────────┐   │
                   │  │   Send via Resend               │   │
                   │  │   (non-blocking)                │   │
                   │  └─────────────────────────────────┘   │
                   └─────────────────────────────────────────┘
```

### Data Payload

```javascript
// Frontend → Backend
{
  name: "John Doe",
  colosseum_name: "CyberGladiator",
  email: "john@example.com",
  timezone: "et",
  stack: "fullstack",
  github: "https://github.com/johndoe",
  x: "https://x.com/johndoe",        // optional
  linkedin: "https://linkedin.com/in/johndoe", // optional
  demo: "yes",
  fairplay: true,
  language: "en"  // from navigator.language
}

// Backend → Database (quickdrop_registrations)
INSERT INTO quickdrop_registrations (
  name, colosseum_name, email, timezone, stack,
  github_url, x_url, linkedin_url, demo_interest,
  fairplay_agreed, drop_id
)
```

---

## 2. Waitlist Flow (Popup)

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Browser   │───▶│WaitlistModal│───▶│ /api/waitlist│───▶│    Neon    │
│   (User)    │    │    .jsx     │    │   route.js   │    │  Database  │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
                                              │
                                              ▼
                   ┌─────────────────────────────────────────┐
                   │  IF role === 'arena':                   │
                   │  ┌─────────────────────────────────┐   │
                   │  │   Create Avatar Token           │   │
                   │  └─────────────────────────────────┘   │
                   └─────────────────────────────────────────┘
```

### Data Payload

```javascript
// Frontend → Backend
{
  name: "Jane Doe",
  email: "jane@example.com",
  role: "arena" | "spectator"
}

// Backend → Database (waitlist_entries)
INSERT INTO waitlist_entries (name, email, role)
```

---

## 3. Welcome Email Flow (Cron)

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Vercel    │───▶│  /api/cron/ │───▶│    Neon    │
│    Cron     │    │send-welcome │    │  Database  │
│  (9 AM EST) │    │  -emails    │    └─────────────┘
└─────────────┘    └─────────────┘           │
                          │                   │
                          │    ┌──────────────┘
                          ▼    ▼
                   ┌─────────────────────────────────────────┐
                   │  FOR EACH pending user:                 │
                   │  1. Get user from waitlist_entries      │
                   │  2. Get drop info from competitions DB  │
                   │  3. Generate content (Groq AI)          │
                   │  4. Send email (Resend)                 │
                   │  5. Update welcome_email_sent = TRUE    │
                   └─────────────────────────────────────────┘
```

---

## 4. Gladiator Search Flow (Dex)

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Browser   │───▶│ Gladiator   │───▶│/api/gladiators│──▶│    Neon    │
│   (User)    │    │ Interface   │    │   route.js   │    │  Database  │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
                                              │
                                              ▼
                   ┌─────────────────────────────────────────┐
                   │  UNION QUERY:                           │
                   │  - quickdrop_registrations              │
                   │  - waitlist_entries (role='arena')      │
                   │  - competitors (historical)             │
                   │  - avatar_tokens (for avatar_url)       │
                   │                                         │
                   │  DEDUPLICATE by email                   │
                   │  ORDER BY wins DESC                     │
                   └─────────────────────────────────────────┘
```

---

## 5. Avatar Token Flow

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Avatar    │───▶│/api/avatar- │───▶│    Neon    │
│  Generator  │    │   token     │    │  Database  │
│  (External) │    │  route.js   │    │            │
└─────────────┘    └─────────────┘    └─────────────┘

GET ?email=user@example.com
→ Returns: { current_token, token_used, is_expired, generation_enabled }

POST { action: 'validate', email, token }
→ Validates and consumes token, returns success/failure
```

---

## Database Write Patterns

| Operation          | Table                                               | Trigger           |
| ------------------ | --------------------------------------------------- | ----------------- |
| Apply Form Submit  | quickdrop_registrations, competitors, avatar_tokens | User action       |
| Waitlist Submit    | waitlist_entries, avatar_tokens (if arena)          | User action       |
| Welcome Email Sent | waitlist_entries.welcome_email_sent                 | Cron job          |
| Token Consumed     | avatar_tokens.token_used                            | External API call |
| Win/Loss Update    | quickdrop_registrations.wins/losses                 | Admin action      |

---

_Last updated: January 20, 2026_
